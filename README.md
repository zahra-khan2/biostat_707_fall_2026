# biostat_707_fall_2026

Checkpoint 1 - September 21, 2026: 
The checkpoint1.qmd runs with the necessary set-a and outcomes-a txt files to be loaded into the workspace where starting with the first library/set-up chunk, followed by check-data, load data, data wrangling, exploratory analyses, missingness plots, and other chunks in sequential order. For rendering purposes, code and results for certain chunks have been hidden for a cleaner outcome if the .qmd file is to be rendered into an html once all code chunks run in sequential order with no error. The output should contain preliminary data frame outputs to assess the data structure which should be hidden in the data rendering, but tibble outputs for exploratory analysis will output in rendered html to showcase results. Following this 3 lasagna plots, 2 proportions of lab variables measured in 48-hours, and measurement rate versus time should be outputted. Following this table 1, outcome summary, missingness map plot and missingness signal table should be outputted, with respective save paths to the output folder. 
